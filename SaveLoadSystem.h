#pragma once
#include <iostream>
#include <vector>
#include "Task.h"

/* 
 *
 * Save/Load System for saving and loading objects in lists
 * 
 */

class SaveLoadSystem {

public:

	void save(std::vector<Task> tasks);

	//vector<Task> load(); 

private:

};
